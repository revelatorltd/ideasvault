# Journal

One line per completed unit, newest last. Dates are UTC.

- 2026-08-25 — `ccaa0e2` Baseline imported from owner's zip: 23 files, spec set verbatim, exec bit set on `scripts/publish.sh`. Verified boot on pinned deps.
- 2026-08-25 — `0d9acc6` M0.1 orientation: 4 of 5 prior-session findings confirmed, 1 withdrawn (F3), 1 new found (F6, reindex 500s on deleted DB). SPEC 2.2 exemption for `revision`, SPEC 6 + 7.14 reworded, PLAN task 2.0 added.
- 2026-08-25 — Protocol adopted. `~/.claude/settings.json` created with `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (user-level, effective next session).
- 2026-08-25 — Orientation phase: `.claude/notes/orientation.md` written; independent 7-area map + adversarial verify workflow launched. `tests/conftest.py` committed as groundwork only — no test file yet, so `pytest` collects nothing until unit 1.
- 2026-08-25 — U1 test suite: 43 tests (21 metadata + 22 app) covering all 14 SPEC §7 requirements plus F1/F2/F4/F6 guards. 39 pass; the 4 failures are exactly the known defects. `requirements-dev.txt` added (pytest, httpx — httpx forced by PLAN M1.1's mandated `fastapi.testclient`). Runtime deps still five. No `app/` code touched.
- 2026-08-25 — U2 fixed F6 (schema ensured per-connection in `db.conn()`, not just at boot — reads 500'd too, wider than SPEC §7.11) and new F7 (`reindex` only inserted, so stale rows made the 410 advice unfollowable; added `db.prune()`). 42 pass, 3 remaining failures are F1/F2/F4.
- 2026-08-25 — U3 fixed F1+F2 with a single rule in `db.upsert`: only a changed sha256 bumps `revision` (and `updated_at`). Also stops reindex shuffling card order via the `updated_at` sort tiebreaker. 44 pass, 1 remaining failure is F4.
- 2026-08-25 — U4 fixed F4: collision guard keys on the file, not the index row (disk is truth), so a rebuilt index can no longer license destroying an artifact. Suffix escalates 6→12→64 hex and refuses rather than overwrite. Full suite 45/45 green.
- 2026-08-25 — U5 (PLAN 2.0) fixed F5 via `profiles: ["edge"]`, added Inter to detail.html, blanked the .env.example token. First attempt added a `:?` guard on CF_TUNNEL_TOKEN which broke `docker compose up` at M2 entirely (compose interpolates all services regardless of profile) — caught by the unit's own verification and replaced with `:-`. 45/45 green.
- 2026-08-25 — Own adversarial pass found F8 (token comparison not constant-time, SPEC §4), F9 (reindex slugs violated SPEC §2.3 for arbitrary filenames), F10 (case-sensitive glob made RESTORED.HTML invisible — invariant 1 failure). All three fixed. Traversal, autoescape and /raw/ path construction checked clean. 50/50 green.
- 2026-08-25 — F11 (bad inbox file logged every poll — ~29k lines/day; now once, measured 1 line per 12 polls) and F12 (typo'd VAULT_VIEWER_LEVEL 500'd every page; now refuses to boot with an actionable message, and the visibility ladder is defined once). 54/54 green.
- 2026-08-25 — U6 partial merge of the sweep: F13 (Unicode digits passed the 'strict' date check), F14 (non-ASCII explicit slug hashed the body, breaking invariant 3), F15 (non-atomic write could destroy the only copy), F16 (corrupt vault.db crash-looped, against SPEC §6), F17 (racing first publish 500'd), F18 (oversized inbox file read before the size check). All fixed. Two findings flagged for an owner decision, not built. 60/60 green.
- 2026-08-25 — Sweep completed (14/14 agents). Verify phase: 6 of 7 verdicts REFUTED as stale re-reports of defects I had already fixed; 1 CONFIRMED — F19, publish ran on the event loop and a legal 15MB artifact measured 65s of CPU, freezing every route including /healthz. Fixed via asyncio.to_thread.
- 2026-08-25 — Two regressions from my OWN earlier fixes, found in the sweep's unverified list and confirmed: F20 (F8's compare_digest raises on non-ASCII, so any unauthenticated caller could turn a 401 into a 500 — verified 500 before / 401 after over a raw socket) and F21 (F7's prune deleted rows for artifacts published mid-reindex, orphaning the file). Both fixed. Also documented `docker compose --profile edge up -d` in the README, which my own profile change had made necessary. 65/65 green.
- 2026-08-25 — Added `.devcontainer/devcontainer.json`. Base image swapped from the supplied `dotnet:8.0` to `python:3.12` (a .NET image cannot run this repo at all); kept the claude-code feature, config volume and CLAUDE_CONFIG_DIR as specified. Added docker-in-docker so PLAN M2.1 is runnable, absolute VAULT_* paths so the app never writes to /data, and port 8000. Verified from a different cwd: healthz 200, publish 201, wrote inside the workspace, /data absent.
